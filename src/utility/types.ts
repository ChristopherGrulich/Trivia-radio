export type AnswerProps = {
        answer: string,
        answerid: any,
        question: string,
        onClick: any,
        toggled: boolean,
        isCorrect: boolean,
        gameOver: boolean,
}

export type CategoryProps = {
    categoryChoice: string,
}